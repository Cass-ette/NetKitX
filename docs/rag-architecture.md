# RAG 知识增强架构

## 概述

RAG（Retrieval-Augmented Generation，检索增强生成）让 AI Agent 能够利用历史攻防经验，不再"失忆"。

**核心思路**：把知识条目转成向量存入数据库，新会话开始时用语义相似度检索相关经验，注入到 System Prompt 中。

## 架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        RAG 知识增强流程                              │
│                                                                     │
│  ┌──────────────── 写入阶段（离线）──────────────────┐              │
│  │                                                    │              │
│  │  Agent 会话结束                                    │              │
│  │       │                                            │              │
│  │       ▼                                            │              │
│  │  ┌──────────┐    AI 提取     ┌──────────────┐     │              │
│  │  │ 会话记录  │ ──────────▶  │ 结构化知识条目  │     │              │
│  │  │ (turns)  │   (Phase 2)   │ scenario      │     │              │
│  │  └──────────┘               │ vulnerability │     │              │
│  │                             │ tools, tags.. │     │              │
│  │                             └──────┬───────┘     │              │
│  │                                    │              │              │
│  │                                    ▼              │              │
│  │                        ┌─────────────────┐        │              │
│  │                        │  Embedding API   │        │              │
│  │                        │ (OpenAI / 智谱)  │        │              │
│  │                        └────────┬────────┘        │              │
│  │                                 │                  │              │
│  │                    "SQL注入 union 绕过登录"         │              │
│  │                          ↓                         │              │
│  │                 [0.12, -0.03, 0.87, ..., 0.45]    │              │
│  │                    (1536维向量)                     │              │
│  │                          │                         │              │
│  │                          ▼                         │              │
│  │               ┌──────────────────┐                │              │
│  │               │   PostgreSQL     │                │              │
│  │               │   + pgvector     │                │              │
│  │               │                  │                │              │
│  │               │  knowledge_entries│               │              │
│  │               │  ┌────────────┐  │                │              │
│  │               │  │ embedding  │  │  ◀── HNSW 索引│              │
│  │               │  │ vector(1536)│  │               │              │
│  │               │  └────────────┘  │                │              │
│  │               └──────────────────┘                │              │
│  └────────────────────────────────────────────────────┘              │
│                                                                     │
│  ┌──────────────── 读取阶段（实时）──────────────────┐              │
│  │                                                    │              │
│  │  用户: "帮我测试这个站的 SQL 注入"                  │              │
│  │       │                                            │              │
│  │       ▼                                            │              │
│  │  ┌──────────────┐                                  │              │
│  │  │ Embedding API │  ──▶  [0.11, -0.02, 0.85, ...]│              │
│  │  └──────┬───────┘                                  │              │
│  │         │                                          │              │
│  │         ▼            余弦相似度                     │              │
│  │  ┌──────────────┐   <=> 运算    ┌──────────────┐  │              │
│  │  │ 查询向量      │ ───────────▶ │  pgvector    │  │              │
│  │  └──────────────┘    TOP 5      │  向量检索     │  │              │
│  │                      ≥0.6       └──────┬───────┘  │              │
│  │                                        │           │              │
│  │         ┌──────────────────────────────┘           │              │
│  │         ▼                                          │              │
│  │  ┌─────────────────────────────────┐               │              │
│  │  │ ## 相关历史经验                   │               │              │
│  │  │                                  │               │              │
│  │  │ ### 经验1: SQL注入登录 (85%)     │               │              │
│  │  │ - 目标类型: web | 漏洞: sqli    │               │              │
│  │  │ - 工具: sqlmap                   │               │              │
│  │  │ - 关键发现: union select 有效    │               │              │
│  │  │ - 结果: success                  │               │              │
│  │  └──────────────┬──────────────────┘               │              │
│  │                 │                                   │              │
│  │                 ▼                                   │              │
│  │  ┌──────────────────────────────────┐              │              │
│  │  │        System Prompt             │              │              │
│  │  │  ┌────────────────────────────┐  │              │              │
│  │  │  │ 你是安全测试 AI Agent...    │  │  ◀── 原有   │              │
│  │  │  └────────────────────────────┘  │              │              │
│  │  │  ┌────────────────────────────┐  │              │              │
│  │  │  │ ## 相关历史经验             │  │  ◀── RAG 注入│              │
│  │  │  │ (上面检索到的内容)          │  │              │              │
│  │  │  └────────────────────────────┘  │              │              │
│  │  └──────────────┬───────────────────┘              │              │
│  │                 │                                   │              │
│  │                 ▼                                   │              │
│  │          ┌─────────────┐                           │              │
│  │          │  AI (LLM)   │  参考历史经验做出更好决策  │              │
│  │          └─────────────┘                           │              │
│  └────────────────────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────────────────┘
```

## 关键代码位置

| 环节 | 文件 | 说明 |
|------|------|------|
| 知识提取后自动 embed | `app/services/knowledge_service.py:651` | extract_knowledge 成功后调 embed |
| 生成向量 + 存 DB | `app/services/embedding_service.py:107` | embed_knowledge_entry() |
| 调 Embedding API | `app/services/embedding_service.py:23` | OpenAI / 智谱 API |
| Agent 启动时检索注入 | `app/services/agent_service.py:492` | run_agent_loop 中注入 RAG |
| pgvector 余弦搜索 | `app/services/embedding_service.py:144` | search_similar_knowledge() |
| 格式化为 prompt 段落 | `app/services/embedding_service.py:201` | format_rag_context() |
| 向量列定义 | `app/models/knowledge.py` | `embedding = Column(Vector(1536))` |
| 数据库索引 | migration `b9c3d5e7f1a2` | HNSW 索引 |

## 配置项

```python
RAG_ENABLED = False                          # 总开关
RAG_EMBEDDING_PROVIDER = ""                  # "openai" | "zhipuai"
RAG_EMBEDDING_API_KEY = ""                   # API 密钥
RAG_EMBEDDING_MODEL = "text-embedding-3-small"  # 模型
RAG_EMBEDDING_DIM = 1536                     # 向量维度
RAG_TOP_K = 5                                # 检索数量
RAG_SIMILARITY_THRESHOLD = 0.6              # 相似度阈值
```

## 一句话总结

> **写入**：每次攻防会话结束 → AI 提取结构化知识 → Embedding API 转向量 → 存入 pgvector
>
> **读取**：新会话开始 → 用户问题转向量 → 余弦相似度检索 TOP 5 → 拼接到 System Prompt → AI 带着历史经验执行
>
> **核心价值：AI Agent 不再失忆，每次攻防都能站在历史经验的肩膀上。**
